import 'package:flutter/material.dart';

class AppColor {
  const AppColor._();
  static const Color primaryColor = Colors.white;
  static const Color secondaryColor = Colors.deepOrange;
  static const Color appBarColor = Colors.white;
  static const Color secondaryButtonColor = Colors.deepOrange;
  static const Color primaryButtonColor = Colors.green;
  static const Color buttonTextColor = Colors.deepOrange;
  static const Color textColor = Colors.black;
}